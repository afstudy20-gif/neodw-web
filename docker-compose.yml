services:
  orthanc:
    image: orthancteam/orthanc:25.4.0
    container_name: remote-dicom-orthanc
    restart: unless-stopped
    expose:
      - "8042"
      - "4242"
    volumes:
      - ./orthanc-data:/var/lib/orthanc/db
      - ./orthanc.json:/etc/orthanc/orthanc.json:ro

  viewer:
    build: .
    container_name: remote-dicom-viewer
    restart: unless-stopped
    expose:
      - "80"
    depends_on:
      - orthanc
